# Xcode
*.xccheckout
*.moved-aside
*.xcuserstate
*.xcscmblueprint
xcuserdata/
DerivedData/
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# SPM
.build/
.swiftpm/
# Package.resolved is the dependency lockfile and is deliberately tracked — an
# earlier `*.resolved` rule here is why the repo never had one.

# Fastlane
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**/*.png
fastlane/test_output
build/

# Ruby / Bundler
vendor/bundle/
.bundle/
Gemfile.lock

# Certificates (never commit)
*.p12
*.cer
*.mobileprovision

# Misc
.DS_Store
*.log
test-output/
TestResults.xcresult
